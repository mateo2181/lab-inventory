{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "name": "orders"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "request_date": {
      "type": "date"
    },
    "status": {
      "type": "enumeration",
      "enum": [
        "requested",
        "finished",
        "cancelled"
      ]
    }
  }
}
